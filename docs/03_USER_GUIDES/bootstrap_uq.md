# Bootstrap & Uncertainty Quantification

This document explains the bootstrap procedure used for uncertainty quantification in the pipeline.

- Default: 200 resamples × 5 seeds (configurable via `--n-boot` and `--seeds`)
- Outputs: `outputs/validation/*`, confidence intervals and stability reports

(Expand with interpretation guidelines and example plots.)