# KDR100 Datenselektion Requirements

# Deep Learning & Image Processing (CPU-only für i5-12500H)
# Install mit: pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
Pillow>=10.0.0

# Data Processing
pandas>=2.0.0
numpy==1.26.4
numba==0.63.1
dbfread>=2.0.7

# Machine Learning & Clustering
scikit-learn>=1.3.0
umap-learn==0.5.11

# Statistical Sampling (LHS for parameter exploration)
scipy>=1.7.0

# Submodular Optimization (Facility Location)
apricot-select==0.6.1

# Hyperparameter Optimization
optuna>=3.0.0
cmaes>=0.10.0  # Required for CmaEsSampler in Optuna

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0

# Experiment Tracking
wandb>=0.15.0

# Testing
pytest>=7.0.0

# Linting & Type Checking
flake8>=6.0.0
mypy>=1.9.0

# Geo / Spatial (pinned; prefer conda-forge install via environment.yml)
# These are optional for CPU-only installs but required for Geo workflows
geopandas==0.15.1
pyproj==3.7.1
shapely==2.0.1
fiona==1.9.4
rtree==0.9.8
rasterio==1.3.8
